# Infant Nook — Landing

Sitio público: https://www.infantnook.com/

Stack: HTML/CSS/JS vanilla, una sola página (`index.html`), desplegado en Vercel desde este repo (rama `main`).

## Versión actual

**Preview v1.7** — Tarjetas casi transparentes (32%) que se solidan al hover con lift + scale.

> Nota: el commit `ff1076c` (2026-04-10) llevaba el mensaje "Update index.html: preview v1.2" pero el contenido real subido ya era v1.7. Este README deja constancia para que el historial sea legible sin tener que abrir el HTML.

## Despliegue

Push a `main` → Vercel hace el deploy automático en ~30 s.
