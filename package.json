{
  "name": "infant-nook-landing",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check app.js && node --check discovery.js && node --check newsletter.js && node --check visual.js && node scripts/build.mjs",
    "dev": "npm run build && python3 -m http.server 4328 --bind 127.0.0.1 --directory dist"
  },
  "engines": {
    "node": ">=22"
  }
}
